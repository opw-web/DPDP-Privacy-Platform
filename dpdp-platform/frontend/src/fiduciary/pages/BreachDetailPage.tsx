import { useState } from "react"; import { useParams } from "react-router-dom"; import { useQuery, useMutation } from "@tanstack/react-query"; import { employeeApiClient } from "../../lib/api-client"; import { Button } from "../../components/ui/button"; import { Card, CardContent, CardHeader, CardTitle } from "../../components/ui/card"; import { BreachObligationCard, type Obligation } from "../components/breaches/BreachObligationCard";
interface Breach { id: string; reference: string; title: string; becameAwareAt: string; boardExtensionGrantedUntil: string | null; boardExtensionReference: string | null; obligations: Obligation[]; }
export function originalBoardDetailDueAt(obligations: Obligation[]): string | null { return obligations.find((o) => o.code === "BOARD_DETAIL")?.dueAt ?? null; }
export function BreachDetailPage() { const { breachId } = useParams(); const q = useQuery({ queryKey: ["breach", breachId], queryFn: () => employeeApiClient.get<Breach>(`/breaches/${breachId}`), enabled: Boolean(breachId) }); const [until, setUntil] = useState(""); const [reference, setReference] = useState(""); const ext = useMutation({ mutationFn: () => employeeApiClient.post(`/breaches/${breachId}/extension`, { grantedUntil: new Date(until).toISOString(), reference }), onSuccess: () => void q.refetch() }); if (!q.data) return <p>Loading breach…</p>; const b = q.data; return <div className="space-y-6"><div><h1 className="text-xl font-semibold">{b.title}</h1><p className="text-sm text-muted-foreground">{b.reference} · awareness clock started {new Date(b.becameAwareAt).toLocaleString()}</p></div><Card><CardHeader><CardTitle>Obligations</CardTitle></CardHeader><CardContent className="space-y-3">{b.obligations.map((o) => <BreachObligationCard key={o.id} breachId={b.id} obligation={o} />)}</CardContent></Card><Card><CardHeader><CardTitle>Board detailed-report extension</CardTitle></CardHeader><CardContent className="space-y-3"><p className="text-sm">Only the detailed-report clock moves. The original date remains preserved and visible.</p>{b.boardExtensionGrantedUntil ? <p className="text-sm">Original date: <s>{new Date(originalBoardDetailDueAt(b.obligations) ?? "").toLocaleString()}</s><br />Extended until {new Date(b.boardExtensionGrantedUntil).toLocaleString()} · reference {b.boardExtensionReference}</p> : <><input aria-label="Extension date" type="datetime-local" className="flex h-9 w-full rounded border px-3" value={until} onChange={(e) => setUntil(e.target.value)} /><input aria-label="Extension reference" className="flex h-9 w-full rounded border px-3" placeholder="Board reference" value={reference} onChange={(e) => setReference(e.target.value)} /><Button disabled={!until || !reference || ext.isPending} onClick={() => ext.mutate()}>Record extension</Button></>}</CardContent></Card></div>; }
