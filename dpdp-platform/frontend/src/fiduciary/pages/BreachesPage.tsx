import { useQuery } from "@tanstack/react-query"; import { Link } from "react-router-dom"; import { employeeApiClient } from "../../lib/api-client"; import { Button } from "../../components/ui/button"; import { Card, CardContent } from "../../components/ui/card";
interface Breach { id: string; reference: string; title: string; status: string; becameAwareAt: string; }
export function BreachesPage() { const q = useQuery({ queryKey: ["breaches"], queryFn: () => employeeApiClient.get<Breach[]>("/breaches") }); return <div className="space-y-6"><div className="flex justify-between"><div><h1 className="text-xl font-semibold">Breach register</h1><p className="text-sm text-muted-foreground">Track awareness clocks, Board obligations and evidence.</p></div><Button asChild><Link to="/app/breaches/new">Record breach</Link></Button></div><div className="grid gap-3">{(q.data ?? []).map((b) => <Card key={b.id}><CardContent className="flex items-center justify-between p-4"><div><p className="font-semibold">{b.title}</p><p className="text-sm text-muted-foreground">{b.reference} · {b.status} · aware {new Date(b.becameAwareAt).toLocaleString()}</p></div><Button variant="outline" asChild><Link to={`/app/breaches/${b.id}`}>Open</Link></Button></CardContent></Card>)}</div></div>; }
